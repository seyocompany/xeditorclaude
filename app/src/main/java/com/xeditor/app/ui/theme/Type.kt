package com.xeditor.app.ui.theme

import androidx.compose.material3.Typography

// The default Material 3 type scale is already tuned for accessibility
// (contrast-safe sizes, sensible line heights), so it's used as-is here
// rather than overridden. A custom font gets introduced later if the
// project needs one for branding.
val XEditorTypography = Typography()
